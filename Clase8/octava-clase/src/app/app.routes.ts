import { Routes } from '@angular/router';
import { TeoriaHttpComponent } from './components/teoria-http/teoria-http.component';

export const routes: Routes = [{ path: '', component: TeoriaHttpComponent }];
